alias -s {css,js,jsx,ts,tsx,html,rb,erb,md,sass,scss}=code
alias addkeys='ssh-add -A'
alias atom='atom-beta'
alias be='bundle exec'
alias blaude="bun run --bun claude"
alias branches='git branch --list'
if [[ $OSTYPE == 'darwin'* ]]; then
  alias cat='bat'
else
  alias cat='batcat'
fi
alias c='clear'
alias c.='code .'
alias cl='claude'
alias clc='claude -c'
alias clr='claude -r'
function _git_default_branch() {
  git symbolic-ref refs/remotes/origin/HEAD 2>/dev/null | sed 's@^refs/remotes/origin/@@' || echo "main"
}
function _git_worktree_branches() {
  git worktree list --porcelain 2>/dev/null | grep '^branch ' | sed 's@^branch refs/heads/@@'
}
alias cleanmerged='git branch --merged origin/$(_git_default_branch) | grep -Ev "^\s*(\*|master|develop|main|prod)" | grep -Fxv "$(_git_worktree_branches)" | xargs -r -n 1 git branch -d'
alias cleangone='git fetch --prune && git branch -vv | grep "\[gone\]" | grep -Ev "^\s*\*" | awk "{print \$1}" | grep -Fxv "$(_git_worktree_branches)" | xargs -r -n 1 git branch -D'
alias cleansquashed='_default=$(_git_default_branch) && _wt=$(_git_worktree_branches) && git for-each-ref refs/heads/ "--format=%(refname:short)" | while read branch; do echo "$_wt" | grep -Fxq "$branch" && continue; mergeBase=$(git merge-base origin/$_default $branch) && [[ $(git cherry origin/$_default $(git commit-tree $(git rev-parse $branch^{tree}) -p $mergeBase -m _)) == "-"* ]] && git branch -D $branch; done'
alias cleanbranches='cleanmerged && cleangone && cleansquashed'
alias d='docker'
alias dc='docker-compose'
alias dce='docker-compose exec'
alias dcew='docker-compose exec web'
alias dcewbe='docker-compose exec web bundle exec'
alias dcef='docker-compose exec frontend'
alias dcefbe='docker-compose exec frontend bundle exec'
alias dev='cd ~/dev'
alias dps='docker ps'
alias fj='pbpaste | jq | pbcopy'
alias g='git'
alias gco='git checkout'
alias gcob='git checkout -b'
alias gcod='git checkout develop'
alias gcodp='git checkout develop && pull'
alias gcom='git checkout master'
alias gcomp='git checkout master && pull'
alias gcon='git checkout main'
alias gconp='git checkout main && pull'
alias glo='git log --oneline'
alias glag='git log --oneline --graph'
alias glog='git log'
alias gst='git status'
alias gx='gitx'
alias install='sudo apt install $1'
alias listeners='lsof -nP -iTCP -sTCP:LISTEN'
alias nrd='npm run dev'
alias pdc='pwd | pbcopy'
alias ping='prettyping --nolegend'
alias pop='git stash pop'
alias pull='git pull --prune && cleanbranches'
alias push='git push'
alias remotes='git remote -v'
alias rmexif='exiftool -all= $1'
alias showtags='npm dist-tag ls $1'
alias stash='git stash -u'
alias temp='vcgencmd measure_temp'
alias tm='tmux'
alias tf='terraform'
alias upgrades='apt list --upgradable'
alias unexif='mogrify -strip'
alias ys='yarn start'
alias yb='yarn build'
alias yd='yt-dlp'
alias z='zed'
alias z.='zed .'
function zup() {
    curl -v --user-agent "zuploader/0.1" -F"file=@$1" http://0x0.st
}

[[ -f ~/.zsh_aliases.local ]] && source ~/.zsh_aliases.local
[[ -f ~/.zsh_aliases.work ]] && source ~/.zsh_aliases.work
